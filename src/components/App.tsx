import cx from 'classnames'

const App = () => {
  return (
    <div
      className={cx(
        'flex',
        'items-center',
        'justify-center',
        'bg-primary',
        'text-white'
      )}
    >
      React App Starter
    </div>
  )
}

export default App
